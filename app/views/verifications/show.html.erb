<div class="max-w-xl mx-auto py-12 px-4">
  <div class="bg-white rounded-lg shadow-lg p-8">
    <div class="text-center mb-8">
      <h1 class="text-2xl font-bold text-gray-900 mb-2">Verify Visa Letter</h1>
      <p class="text-gray-600">Enter the verification code from your visa invitation letter to confirm its authenticity.</p>
    </div>

    <%= form_with url: verify_path, method: :post, class: "space-y-6", data: { turbo: false } do %>
      <div>
        <%= label_tag :code, "Verification Code", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= text_field_tag :code, @verification_code,
            class: "w-full px-4 py-3 border border-gray-300 rounded-md text-center text-lg font-mono tracking-wider focus:ring-2 focus:ring-red-500 focus:border-red-500",
            placeholder: "Enter 32-character code",
            autofocus: true %>
      </div>

      <%= submit_tag "Verify Letter", class: "w-full bg-red-600 text-white py-3 px-4 rounded-md hover:bg-red-700 font-medium cursor-pointer" %>
    <% end %>

    <div class="mt-8 pt-6 border-t border-gray-200 text-center text-sm text-gray-500">
      <p>This verification system confirms that a visa invitation letter was officially issued by The Hack Foundation.</p>
    </div>
  </div>
</div>

<div class="bg-blue-900 text-white py-12 px-6 mb-8">
  <div class="max-w-4xl mx-auto text-center">
    <h1 class="text-3xl font-bold mb-4">Need a Visa to come to the United States?</h1>
    <p class="text-lg text-gray-300">Apply for an official visa invitation letter for Hack Club events. These letters support your visa application to attend our events in the United States.</p>
    <p class="text-lg text-gray-300 mt-4">Please check the specific program / YSWS website to learn more about the requirements to apply for a visa letter. Your request for a visa letter will be sent to your YSWS / program for approval.</p>
  </div>
</div>

<div class="bg-blue-100 border-l-4 border-blue-700 p-4 mb-8">
  <div class="flex">
    <div class="flex-shrink-0">
      <svg class="h-5 w-5 text-blue-700" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
      </svg>
    </div>
    <div class="ml-3">
      <p class="text-sm text-blue-900">
        <strong>Important:</strong> A visa invitation letter does not guarantee visa approval. It is one of several supporting documents you may need for your visa application. Please consult your local U.S. Embassy or Consulate for complete visa requirements.
      </p>
    </div>
  </div>
</div>

<h2 class="text-2xl font-bold text-blue-900 mb-6 border-b-2 border-blue-700 pb-2">Available Events</h2>

<% if @events.any? %>
  <div class="space-y-6">
    <% @events.each do |event| %>
      <div class="bg-white border border-gray-300 shadow-sm overflow-hidden">
        <div class="border-l-4 border-blue-700 p-6">
          <div class="flex justify-between items-start">
            <div class="flex-1">
              <h3 class="text-xl font-bold text-blue-900 mb-2"><%= event.name %></h3>
              <dl class="grid grid-cols-1 md:grid-cols-2 gap-2 text-sm text-gray-700 mb-4">
                <div><dt class="inline font-semibold">Event Dates:</dt> <dd class="inline"><%= event.date_range %></dd></div>
                <div><dt class="inline font-semibold">Location:</dt> <dd class="inline"><%= event.city %>, <%= event.country %></dd></div>
                <div><dt class="inline font-semibold">Venue:</dt> <dd class="inline"><%= event.venue_name %></dd></div>
                <% if event.application_deadline.present? %>
                  <div><dt class="inline font-semibold">Application Deadline:</dt> <dd class="inline text-red-700"><%= event.application_deadline.strftime("%B %d, %Y") %></dd></div>
                <% end %>
              </dl>
              <% if event.description.present? %>
                <p class="text-gray-600 text-sm"><%= truncate(event.description, length: 200) %></p>
              <% end %>
            </div>
            <div class="ml-6">
              <%= link_to "Apply Now →", new_event_application_path(event_slug: event.slug),
                          class: "inline-block bg-blue-700 text-white py-3 px-6 font-semibold hover:bg-blue-900 transition-colors" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="bg-yellow-100 border-l-4 border-yellow-500 p-6">
    <h3 class="text-lg font-bold text-yellow-800">No Events Available</h3>
    <p class="text-yellow-700 mt-2">There are currently no events accepting visa letter applications. Please check back later for upcoming events.</p>
  </div>
<% end %>

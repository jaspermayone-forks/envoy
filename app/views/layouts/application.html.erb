<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hack Club Travel Ready Centre" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <style>
      h1, h2, h3, h4, h5, h6 { font-family: 'Libre Baskerville', Georgia, 'Times New Roman', Times, serif !important; }
      
      input[type="text"],
      input[type="email"],
      input[type="tel"],
      input[type="date"],
      input[type="number"],
      input[type="password"],
      select,
      textarea {
        background-color: #fff !important;
        border: 1px solid #d1d5db !important;
        border-radius: 0 !important;
        padding: 0.75rem 1rem !important;
        font-size: 1rem !important;
        line-height: 1.5 !important;
        color: #111827 !important;
        width: 100% !important;
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out !important;
      }
      
      input[type="text"]:focus,
      input[type="email"]:focus,
      input[type="tel"]:focus,
      input[type="date"]:focus,
      input[type="number"]:focus,
      input[type="password"]:focus,
      select:focus,
      textarea:focus {
        outline: none !important;
        border-color: #3b82f6 !important;
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.25) !important;
      }
      
      input::placeholder,
      textarea::placeholder {
        color: #9ca3af !important;
      }
    </style>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="bg-gray-100 min-h-screen flex flex-col">
    <% if admin_signed_in? %>
    <div class="bg-blue-900 text-white py-1 px-4 text-sm">
      <div class="max-w-7xl mx-auto flex justify-end space-x-4">
        <%= link_to "Admin Dashboard", admin_dashboard_path, class: "hover:underline" %>
        <%= button_to "Sign Out", destroy_admin_session_path, method: :delete, class: "hover:underline" %>
      </div>
    </div>
    <% end %>
    <nav class="bg-white border-b-4 border-blue-700 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-20">
          <div class="flex items-center">
            <%= link_to root_path do %>
              <img src="/logo.svg?v=2" alt="Hack Club" class="h-12">
            <% end %>
          </div>
          <div class="flex items-center space-x-6">
            <%= link_to "Home", events_path, class: "text-blue-900 font-semibold hover:underline" %>
            <%= link_to "Check Application Status", lookup_application_path, class: "text-blue-900 font-semibold hover:underline" %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex-grow">
      <%= yield %>
    </main>

    <footer class="bg-blue-900 text-white mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div>
            <h3 class="font-bold mb-2">Hack Club</h3>
            <p class="text-sm text-gray-300">A nonprofit network of high school coding clubs and makers around the world.</p>
          </div>
          <div>
            <h3 class="font-bold mb-2">Contact</h3>
            <p class="text-sm text-gray-300">events@hackclub.com</p>
            <p class="text-sm text-gray-300">Toll free: <a href="tel:1-855-625-4225" class="hover:underline">1-855-625-HACK</a></p>
          </div>
          <div>
            <h3 class="font-bold mb-2">Additional Resources</h3>
            <ul class="text-sm text-gray-300 space-y-1">
              <li><%= link_to "Verify Letter", verify_path, class: "hover:underline" %></li>
              <li><%= link_to "Frequently Asked Questions", documentation_path, class: "hover:underline" %></li>
              <li><%= link_to "Admin Login", new_admin_session_path, class: "hover:underline" %></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-600 mt-6 pt-6 text-center text-sm text-gray-400">
          &copy; <%= Date.current.year %> The Hack Foundation (EIN: 81-2908499). All rights reserved.
        </div>
      </div>
    </footer>
  </body>
</html>
